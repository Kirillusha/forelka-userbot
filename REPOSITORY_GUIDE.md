# 📦 Руководство по загрузке модулей из репозиториев

## 🎯 Обзор

Forelka Userbot теперь поддерживает загрузку модулей напрямую из GitHub репозиториев! Это позволяет:

- ✅ Загружать несколько модулей одной командой
- ✅ Автоматически обновлять модули при повторной загрузке
- ✅ Легко делиться наборами модулей с сообществом
- ✅ Поддерживать различные структуры репозиториев

## 🚀 Быстрый старт

### 1. Загрузка всех модулей из репозитория

```
.dlmr https://github.com/username/repository
```

Эта команда:
1. Клонирует репозиторий в папку `repositories/`
2. Найдет все Python файлы (`.py`)
3. Скопирует их в `loaded_modules/`
4. Загрузит все найденные модули

### 2. Загрузка конкретного модуля из репозитория

```
.dlmr https://github.com/username/repository module_name
```

Эта команда загрузит только указанный модуль из репозитория.

### 3. Просмотр загруженных репозиториев

```
.lmr
```

Показывает:
- Название репозитория
- Количество модулей в нем
- URL репозитория

### 4. Удаление репозитория

```
.rmr repository_name
```

Удаляет клонированный репозиторий из папки `repositories/`.

**⚠️ Примечание:** Это не удалит загруженные модули из `loaded_modules/`. Используйте `.ulm <module_name>` для удаления конкретных модулей.

## 📋 Примеры использования

### Пример 1: Загрузка набора развлекательных модулей

```
.dlmr https://github.com/user/fun-modules
```

**Результат:**
```
✅ Repository fun-modules processed

Loaded (3):
➡️ meme
➡️ joke
➡️ dice

Failed (0):
```

### Пример 2: Загрузка конкретного модуля

```
.dlmr https://github.com/user/utils-modules calculator
```

**Результат:**
```
✅ Module calculator loaded from repository utils-modules
```

### Пример 3: Обновление репозитория

При повторном выполнении команды репозиторий автоматически обновится:

```
.dlmr https://github.com/user/my-modules
```

**Результат:**
```
⌛️ Updating repository my-modules...
✅ Repository my-modules processed
...
```

### Пример 4: Просмотр репозиториев

```
.lmr
```

**Результат:**
```
👻 Downloaded Repositories

➡️ fun-modules
  Modules: 3
  URL: https://github.com/user/fun-modules

➡️ utils-modules
  Modules: 5
  URL: https://github.com/user/utils-modules
```

## 🛡️ Защита системных модулей

Следующие модули защищены от перезаписи:
- `loader` - система загрузки модулей
- `main` - основной файл
- Все модули из папки `modules/` (встроенные системные модули)

При попытке загрузить защищенный модуль вы увидите:
```
❌ Access Denied
```

## 📁 Структура директорий

После работы с репозиториями у вас будет следующая структура:

```
forelka-userbot/
├── modules/              # Системные модули (защищены)
│   ├── help.py
│   ├── ping.py
│   └── ...
├── loaded_modules/       # Загруженные модули
│   ├── meme.py
│   ├── calculator.py
│   └── ...
├── repositories/         # Клонированные репозитории
│   ├── fun-modules/
│   │   ├── .git/
│   │   ├── meme.py
│   │   └── joke.py
│   └── utils-modules/
│       ├── .git/
│       ├── calculator.py
│       └── converter.py
└── ...
```

## 🔄 Workflow: От создания до использования

### Для разработчиков модулей:

1. **Создайте репозиторий на GitHub**
   ```bash
   mkdir my-forelka-modules
   cd my-forelka-modules
   git init
   ```

2. **Добавьте модули**
   ```bash
   # Создайте модули (см. MODULES.md для примеров)
   touch weather.py
   touch meme.py
   ```

3. **Добавьте README**
   ```markdown
   # My Forelka Modules
   
   ## Installation
   .dlmr https://github.com/username/my-forelka-modules
   
   ## Modules
   - weather - Get weather info
   - meme - Random memes
   ```

4. **Запушьте в GitHub**
   ```bash
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/username/my-forelka-modules.git
   git push -u origin main
   ```

### Для пользователей:

1. **Установите модули**
   ```
   .dlmr https://github.com/username/my-forelka-modules
   ```

2. **Проверьте установку**
   ```
   .lm
   ```

3. **Используйте модули**
   ```
   .weather Moscow
   .meme
   ```

4. **Обновите при необходимости**
   ```
   .dlmr https://github.com/username/my-forelka-modules
   ```

## 🐛 Решение проблем

### Проблема: "Git Error: ..."

**Решение:**
- Убедитесь, что git установлен: `git --version`
- Проверьте доступность репозитория
- Проверьте правильность URL

### Проблема: "No Python modules found in repository"

**Решение:**
- Убедитесь, что в репозитории есть `.py` файлы
- Проверьте, что файлы не начинаются с `_` (например, `__init__.py` игнорируется)
- Убедитесь, что репозиторий не пустой

### Проблема: "Module X not found in repository"

**Решение:**
- Проверьте правильность написания имени модуля
- Просмотрите содержимое репозитория на GitHub
- Имя модуля должно совпадать с именем файла (без `.py`)

### Проблема: Модуль не работает после загрузки

**Решение:**
1. Проверьте, что модуль имеет функцию `register()`
2. Убедитесь, что все зависимости установлены
3. Проверьте логи: `tail -f forelka.log`
4. Попробуйте перезапустить юзербота

## 💡 Полезные советы

1. **Используйте короткие имена репозиториев** для удобства
2. **Группируйте похожие модули** в одном репозитории
3. **Добавляйте requirements.txt** если модули используют внешние библиотеки
4. **Версионируйте репозитории** через git tags
5. **Документируйте модули** в README репозитория
6. **Тестируйте перед публикацией** на своем юзерботе

## 🔗 Полезные ссылки

- [Документация по созданию модулей](MODULES.md)
- [Канал Forelka](https://t.me/forelkauserbots)
- [Поддержка](https://t.me/forelusersupport)

## 🎓 Примеры команд

```bash
# Загрузить все модули из репозитория
.dlmr https://github.com/forelka/official-modules

# Загрузить только модуль weather
.dlmr https://github.com/forelka/official-modules weather

# Посмотреть список репозиториев
.lmr

# Удалить репозиторий
.rmr official-modules

# Посмотреть загруженные модули
.lm

# Удалить конкретный модуль
.ulm weather

# Получить файл модуля
.ml weather
```

## 🌟 Создайте свою коллекцию!

Создайте свой репозиторий с модулями и поделитесь с сообществом Forelka!

**Шаблон репозитория:**
```
my-modules/
├── README.md          # Описание и инструкция
├── requirements.txt   # Зависимости (если нужны)
├── module1.py        # Ваш модуль 1
├── module2.py        # Ваш модуль 2
└── module3.py        # Ваш модуль 3
```

Удачной разработки! 👻
